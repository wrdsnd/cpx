import { extendTheme } from '@chakra-ui/react'

export const appTheme = extendTheme({
  fonts: {
    body: 'Inter var, system-ui, sans-serif',
    heading: 'Inter var, system-ui, sans-serif',
    mono: 'Inter var, system-ui, sans-serif',
  },
})
